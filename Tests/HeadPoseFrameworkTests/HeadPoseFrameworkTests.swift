import XCTest
@testable import HeadPoseFramework

final class HeadPoseFrameworkTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(HeadPoseFramework().text, "Hello, World!")
    }
}
